workload_seed: 42

reps: 5
candidate_rpu_values: [4, 8, 16, 32]
queries_per_observation_window_values: [16, 32, 64, 128, 256]

initial_rpus: [16, 16, 16, 16]

slo_resolver_config:
  slo_s: 10.0

slo_objective_config:
  slo_metric: relative
  slo_threshold: 0.0

query_router_config:
  routing_policy_name: use_iconq_model
  iconq_model_id: model_v6

autoscaler_config:
  autoscaling_policy: add_single_best_forward
  observation_window_s: 300.0
  min_finished_queries_in_counterfactual: 30
