workload_seed: 42

reps: 1
candidate_rpu_values: [4, 8, 16, 32]
arrival_rate_qps_values: [0.1, 0.25, 0.5, 0.75, 1]

initial_rpus: [16, 16, 16, 16]

slo_resolver_config:
  slo_s: 10.0

slo_objective_config:
  slo_metric: relative
  slo_threshold: 0.0

query_router_config:
  routing_policy_name: use_iconq_model
  iconq_model_id: all_66_and_99_template_runs

autoscaler_config:
  autoscaling_policy: add_single_best_forward
  observation_window_s: 120.0
  min_finished_queries_in_counterfactual: 30
